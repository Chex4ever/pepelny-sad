name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    if: "!contains(github.event.head_commit.message, '[skip ci]')"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -r requirements-dev.txt
          sudo apt-get update
          sudo apt-get install -y libgl1-mesa-dri libegl1-mesa xvfb

      - name: Prepare audio assets
        run: python tools/prepare_audio.py

      - name: Run tests
        run: python -m pytest tests/ -q

      - name: Performance budgets (16 ms GPU gameplay gate)
        env:
          LIBGL_ALWAYS_SOFTWARE: "1"
          PEPELNY_RENDER: gpu
        run: xvfb-run -a python -m pytest tests/performance/ -q
